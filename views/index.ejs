<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Application</title>
  </head>
  <body>
    <ul class="company">
      <% for(var i=0; i < 1; i++) { %>
        <div style="width:500px;height:100px;border:1px solid #000;"><p><%= company[0].name %></p>
            <p><%= company[0].tickerCode %></p></div>
      <% } %>
        <!-- <%= latestPrice %> -->
    </ul>
  </body>
</html>
